---
name: print-clothes-sku-generator
description: 印花服饰编码生成器（Excel VBA / BigSeller）：把印花+款式+颜色+尺码自动展开成 BigSeller 单品/组合装 SKU 表。当用户提到"编码生成器"、"印花服饰"、"生成器.txt"、SKU 生成、BigSeller 商品资料表、Sheet8/9/10/11/12/13，或要求修改/排查这个 VBA 宏时使用。
whenToUse: 用户请求涉及印花服饰编码生成器的读码、改码、查 bug、加功能，或询问 SKU 编码规则、BigSeller 导入表字段时（生成器源码已随本 skill 打包在 assets/ 目录，相对路径以本 skill 目录为基准解析）。
metadata:
  source: 由历史工作日志提炼整理（日志未随包；本 skill 内容自包含，不依赖外部文件）
  updated: 2026-08-16
---

# 印花服饰编码生成器 — 操作手册

## 1. 项目是什么

一个 **Shopee/BigSeller 印花服饰 SKU 编码自动生成器**（纯 Excel VBA）。用户在"填写表"里输入产品的印花、款式、颜色、尺码，宏自动校验数据、生成 SKU 编码，并导出成 BigSeller 系统可直接导入的商品资料表（单品表 + 组合装表）。

**相关文件（已随本 skill 打包，相对路径以本 skill 目录为基准）：**
- `assets/越南男装编码生成器源码.txt` — **越南仓男装版**（纯色代号 `M`，分类 `Print`/`Clothes`，组合分类留空）
- `assets/泰国男装编码生成器源码.txt` — **泰国仓男装版**（纯色代号 `TM`，分类 `泰国Print`/`泰国Clothes`/`泰国SKU`）

> 注：早期版本（`编码生成器.txt`、`生成器.txt`）已移除，不再随包；历史 Excel 汉化产物与本生成器无关，未打包。

## 2. 表结构（工作簿内工作表）

| 表 | 角色 | 列结构 |
|---|---|---|
| **Sheet8** | 🎨 颜色库 | A=短编码, B=颜色代码 |
| **Sheet9** | 🖨️ 印花库 | A=短编码, B=印花代码, D=印花名称 |
| **Sheet10** | 👕 款式库 | A=短编码, B=款式代码, C=允许颜色(`/`分隔), D=颜色名称(`/`分隔), E=允许尺码(`/`分隔), F=款式名称 |
| **Sheet13** | 📝 **填写表（核心输入）** | A=印花, B=款式, C=颜色, D=尺码, E=生成的编码, G2=语言标记(Chinese/English) |
| **Sheet11** | 📊 输出·单品表 | A=SKU编号, B=名称, C=分类, D=GTIN, E=仓库, F=库存, G=成本价, H=参考成本价, I=参考售价, J=起售日期, K=重量, L/M/N=长宽高, O=Image URL, P=商品备注 |
| **Sheet12** | 📊 输出·组合装表 | A=*SKU编号, B=*名称, C=仓库, D=分类, E=参考成本价, F=参考售价, G=Image URL, H=商品备注, **I 起每 3 列一个子SKU**（I=*单品SKU1, J=*SKU1数量, K=SKU1成本价分摊比, L=单品SKU2, M=SKU2数量, N=…，支持到 20 个子SKU，到列 BP） |

## 3. 编码语法约定

- **`+` = 组合部件**：印花 `A+B` 表示"前片印花A + 后片印花B"。每一组组合里 印花个数 = 款式个数 = 颜色个数，否则校验报错。
- **`/` = 变体**：颜色 `红/蓝` 表示同一款式可选红或蓝两种。
- **SKU 结构层级**：`印花代号`（如 S008）和 `纯色衣服sku`（如 M-LWDX-WH-160）都是**单个sku**；`印花 + 纯色衣服` = **单件组合sku**（如 S008-LWDX-WH-160）；`多个印花 + 多件纯色衣服`（`+` 连接） = **多件组合sku**（如 TS8-LWDLWD-WHWH-160）。
- **纯色印花代号按仓库区分**：
  - **越南仓**：**T = 童装纯色**、**M = 男装纯色**
  - **泰国仓**：**TT = 童装纯色**、**TM = 男装纯色**（泰国仓 = 越南仓代号前加 `T` 作区分）
  - 填写表印花列填对应代号表示无印花的纯色款。例：泰国仓男装纯色单件 `TM-LWDX-WH-160`。
- **单件 SKU 规范**：`印花代号-款式代号-颜色代号-尺码`，用**全称代号**。纯色件印花代号按仓库填 T/M 或 TT/TM；有印花的单件直接写印花代号。例：越南仓 `T-LWDX-WH-160`（童装纯色-螺纹短袖-白色-160）、`S008-LWDX-WH-160`（两颗心K印花-螺纹短袖-白色-160）。
- **组合 SKU 规范**：`印花1缩写 印花2缩写 - 款式1缩写 款式2缩写 - 颜色1缩写 颜色2缩写 - 尺码`。每维内各部件**缩写直接连写（无分隔符）**，维度间用 `-`；**尺码只有一个**（组合装各件尺码相同，不存在一大一小）。例：越南仓纯色 T + 印花 S008 两件组合 = `TS8-LWDLWD-WHWH-160`。
- **缩写规则**：来自各库 A 列短编码，如 S008→S8、LWDX→LWD、WH→WH（本身不缩写）。**不确定某代号的缩写时先问用户，不要猜。**
- **代码层"无印花"标记**：宏里硬编码 `dicyh("M") = "纯色"` 与 `dicyh("TM") = "纯色"`，所有"无印花"判断同时认 `M` 和 `TM`（注意与业务代号 T/M 的差异）。
- **单品衣服行前缀 `TM-`**：生成器输出（creatMasterProduct/creatCombinedProduct 的子SKU）以 `TM-` 开头，如 `TM-X-红-S`。

## 4. 4 个子程序

```
checktabulation()       校验填写表：印花/款式/颜色查库存在性 + 组合项个数一致 + 颜色尺码是否属于该款式的允许列表
creatsku()              生成编码 → 写入 Sheet13.E 列（每 颜色×尺码 展开一行）
creatMasterProduct()    生成 BigSeller 单品表 → Sheet11（先建 dicyh/dicks/dicys 三个字典；再输出 Print 行 + Clothes 行；最后 RemoveDuplicates 去重）
creatCombinedProduct()  生成 BigSeller 组合装表 → Sheet12（每 颜色×尺码 一个组合装，子SKU = 服装单品 `TM-款式-颜色-尺码` + 印花单品；最后去重）
```

字典定义：
- `dicyh`：印花代码 → 印花名称（Sheet9 B→D；另加 `M`/`TM` → "纯色"）
- `dicks`：款式代码 → 款式名称（Sheet10 B→F）
- `dicys`：`款式代码 & 颜色代码` → 颜色名称（Sheet10 B + C 拆分 → D 拆分）

## 5. 已应用修改（当前两份正式源码的状态，勿回退）

| # | 修改 | 说明 |
|---|---|---|
| 1 | `endsheet10 = Sheet10.[b1048576].End(3).Row` | 原误写 `Sheet9`，导致印花库数据少时款式字典为空、宏中途崩溃 |
| 2 | `startrowRl = 9` | 原为 `8`，子SKU 会错位写入"商品备注"列 |
| 3 | `"M-"` → `"TM-"` | 单品表 Clothes 行编码与组合装子SKU 的前缀 |
| 4 | `dicyh("TM") = "纯色"` | 两处（master + combined），并让所有 `= "M"` 的无印花判断扩展为 `= "M" Or = "TM"` |
| 5 | 仓库列 = `"泰国仓"` | Sheet11 E 列（Print/Clothes 行）与 Sheet12 C 列 |
| 6 | 分类列 | 按实际源码修正：泰国仓印花行=`"泰国Print"`、衣服行=`"泰国Clothes"`（无空格）、组合装 D 列=`"泰国SKU"`；越南仓为 `"Print"`/`"Clothes"` 且组合分类留空 |

## 6. 已知坑位（改动时注意）

- **VBA Dictionary 访问不存在的 key 会抛运行时错误并中断宏**（不是返回空）。`dicyh/dicks/dicys` 查询前建议先 `.Exists(key)` 保护，找不到时回退用原始代码。用户曾遇到"印花库只有一条数据时只生成第一行"就是这个原因。
- **业务代号 vs 代码硬编码不一致**：纯色代号按仓库分——越南仓 **T(童装)/M(男装)**，泰国仓 **TT(童装)/TM(男装)**；宏里硬编码的"无印花"标记只有 `"M"`/`"TM"`（`dicyh("M")="纯色"`、`dicyh("TM")="纯色"`，恰好对应两仓的**男装**纯色）。**T 和 TT（童装纯色）未被硬编码**，若填表用 T/TT，宏会当真实印花处理——需确认 Sheet9 库里有对应记录或补硬编码。填表、改码、核对结果时注意。
- **输出表清空范围不足**：`creatMasterProduct()` 用 `Sheet11.Range("a1:o" & startrow1).ClearContents` 只清到 O 列，但表头写到 P 列（商品备注），P 列会残留旧数据。
- 死代码（不影响功能）：`startrow4`/`startrow5` 赋值未用、`If kscode = "DK" Then End If` 空块、`stcode` 计算后未用。
- `startrow2 = Sheet13.[b1048576].End(3).Row` 以 **B 列**为准确定填写表行数——B 列若断行会漏处理。
- 组合装生成条件：`If Not UBound(strmup) = 0 Or Not printing = "M" And Not printing = "TM"` —— 单一款式 + 纯色时不生成组合装（视为无需捆绑）。

## 7. 作者使用偏好（可遵循，非强制）

- 全程中文交流。
- 改代码前先定位具体行号（可用 findstr /n 定位），改完给出"改动前/后"对照表和影响说明。
- 越南语字段需翻译成准确的中文电商术语（如 Đã Chấp Thuận Yêu Cầu = 请求已批准、Phí Hạ Tầng = 基础设施费用）。
- 拼组合 SKU 时，**如果不知道某个代号的缩写（短编码），先问用户**，不要凭猜。

## 8. 多仓库差异点与扩展方法（男装版源码对比）

越南/泰国两份男装源码约 95% 相同，差异集中在 **8 个可替换参数点**：

| # | 参数点 | 越南仓（男装） | 泰国仓（男装） | 代码位置 |
|---|---|---|---|---|
| 1 | 纯色代号（无印花标记） | `M` | `TM` | master+combined 全部 `= "M"`/`= "TM"` 判断（各 4 处） |
| 2 | 纯色字典硬编码 | `dicyh("M") = "纯色"` | `dicyh("TM") = "纯色"` | master / combined 各 1 处 |
| 3 | 纯色衣服 SKU 前缀 | `"M-"` | `"TM-"` | master Clothes code；combined 子SKU 写入 |
| 4 | 仓库名 | `"越南仓"` | `"泰国仓"` | master Print/Clothes 行 E 列；combined C 列 |
| 5 | 印花分类 | `"Print"` | `"泰国Print"` | master C 列（Print 行） |
| 6 | 衣服分类 | `"Clothes"` | `"泰国Clothes"` | master C 列（Clothes 行） |
| 7 | 组合装分类（D 列） | **留空**（注释弃用，BigSeller 归未分类） | `"泰国SKU"` | combined D 列 |
| 8 | 注释 | 无 | 新增 `'TM为纯色` 等注释（约多 11 行） | — |

**完全相同部分**：checktabulation / creatsku / 表头 / 字典构建结构 / Sheet12 子SKU 每 3 列布局（I 起：SKU 码、+1 数量、+3 跳过分摊比）/ RemoveDuplicates 去重。

**反推扩展新国家仓（模板法）**：
1. 以越南版或泰国版为模板复制一份。
2. 全局替换上表 8 个参数点（纯色代号、前缀、仓库名、印花/衣服/组合分类、字典硬编码键）。
3. **童装版同构**：男装代号把 `M` 换成 `T`（越南男装 M → 童装 T；泰国男装 TM → 童装 TT），SKU 前缀同步。
4. 新国家的具体代号/分类命名规则需向用户确认，不要猜。

**源码中发现的小瑕疵（处理状态）**：
- ✅ 泰国版 checktabulation 印花空提示文案已同步为 `'TM'`（2026-08-16 修复，两份 txt 已同步）。
- ⏸️ checktabulation 无纯色字典兜底——**用户决定不改**：两仓印花库 Sheet9 均已包含纯色代号记录（越南 `M`、泰国 `TM`），检查时能查到并直接放行，日常不会触发。仅当库中缺失该行时检查才会误报（生成端有 `dicyh("M")/"TM"="纯色"` 兜底仍能跑）。若未来想统一口径，加一行 `If printmup(k) = "M"/"TM" Then GoTo skip1` 即可。注意：**童装版 T/TT 生成端也没有兜底**，做童装版需库里加记录或生成端补硬编码。
- 越南版组合装分类留空的原因（源码注释）：系统未预置该分类会归到"未分类"，故弃用（不需处理）。

## 9. 更新记录（Changelog）

- **2026-08-16** 改名：`vba-sku-generator` → `print-clothes-sku-generator`（kebab-case 合规；原 `print&clothes-sku-generator` 因含 `&` 不被 DSH 接受）。
- **2026-08-16** 清理：移除已废弃的早期版本，assets 仅保留两份正式单仓源码；全文去除机器相关绝对路径，改为相对引用（可移植、自包含）。
- **2026-08-16** 源码解析：新增越南/泰国男装源码，提炼 8 个参数化差异点与"模板法"扩展新仓库；修正分类名（泰国 `泰国Print`/`泰国Clothes`/`泰国SKU`）；补充 SKU 结构层级（单个sku → 单件组合 → 多件组合）。
- **2026-08-16** 规范补充：纯色代号按仓库区分（越南 `T`/`M`、泰国 `TT`/`TM`）；单件与组合 SKU 格式；缩写规则（不确定先问作者）。
- **2026-08-16** checktabulation 处理结论：泰国版提示文案同步为 `'TM'`；"纯色字典兜底不一致"已知但决定不改（印花库含 M/TM 记录，日常不触发）。
- **2026-08-16** 封装：技能命名统一为"印花服饰编码生成器"。
